name: Build APK - Controle de Gastos

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      # Passo 1: Checkout do código do repositório
      - name: Checar o código do repositório
        uses: actions/checkout@v2

      # Passo 2: Instalar dependências de build
      - name: Instalar dependências de build
        run: |
          sudo apt-get install -y build-essential python3

      # Passo 3: Instalar o Capacitor
      - name: Instalar o Capacitor
        run: |
          npm install @capacitor/core @capacitor/cli

      # Passo 4: Limpar dependências e reinstalar
      - name: Limpar dependências e reinstalar
        run: |
          rm -rf node_modules
          npm install

      # Passo 5: Instalar a versão mais recente do node-gyp
      - name: Instalar node-gyp
        run: |
          npm install -g node-gyp

      # Passo 6: Configuração do Node.js
      - name: Configurar Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'

      # Passo 7: Verificar instalação do npx
      - name: Verificar instalação do npx
        run: |
          npm bin -g
          npm install -g npx

      # Passo 8: Sincronizar o Capacitor com o Android
      - name: Sincronizar Capacitor com Android
        run: |
          npx cap sync android

      # Passo 9: Abrir o projeto no Android Studio (opcional)
      - name: Abrir no Android Studio
        run: |
          npx cap open android
